# Symbolic Solver Configuration
# Configuration file for logic inference engine

# Dataset configuration
dataset:
  name: "ProofWriter"  # Available options: ProntoQA, ProofWriter, FOLIO, LogicalDeduction

paths:
  input_file: "outputs/gpt/ProofWriter/translation/tur1/translation_results.json"
  output_file: "outputs/gpt/ProofWriter/symbolic_solver/tur1/results.json"
  backup_LLM_result_path: ""

solver:
  backup_strategy: "random"  # choices: ["random", "LLM"]

processing:
  cleanup_cache: true